node_modules/
openapi.json
*.tsbuildinfo

# The built admin is not committed, but the directory is: the Go build
# embeds it, and `go build ./...` has to work on a machine with no Node.
dist/*
!dist/.gitkeep
